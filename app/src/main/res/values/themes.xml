<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BookManager" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bar_color</item>
        <item name="colorPrimaryVariant">@color/bar_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_color</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColorSecondary">@color/main_color</item>
    </style>

    <!--溢出菜单不覆盖焦点-->
    <style name="OverflowMenuTheme" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
    </style>

    <!--NavigationView选中与未选中-->
    <style name="NavigationViewButton" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/main_color</item>
        <item name="android:textColorSecondary">@color/book_card</item>
    </style>
</resources>